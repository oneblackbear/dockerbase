# A General Purpose Server Base

This installs and configures the following:

Nginx
PHP Latest Version
Mysql (MariaDB)
PHP-FPM

A shared mysql data volume is created which allows data to persist through container rebuilds.

